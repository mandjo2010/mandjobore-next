export { 
  useMinimalUIStore as useUIStore,
  useMinimalNavigatorActions as useNavigatorActions,
  useMinimalNavigatorState as useNavigatorState,
  useMinimalFilters as useFilters,
  useMinimalPreferences as usePreferences,
  useMinimalScrollActions as useScrollActions,
} from './ui-minimal'

export type { 
  // Exposer les types minimaux si nécessaire
} from './ui-minimal'
